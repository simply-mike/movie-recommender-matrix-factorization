# Data

This project uses MovieLens 100k.

The notebook downloads and extracts the dataset automatically if it is not found locally.

Expected local layout after download:

```text
data/
└── ml-100k/
    ├── u.data
    ├── u.item
    └── ...
```

The dataset itself should not be committed to git.
